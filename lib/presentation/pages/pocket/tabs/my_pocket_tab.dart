import 'package:flutter/material.dart';

class MyPocketTab extends StatelessWidget {
  const MyPocketTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('나의 포켓'));
  }
}
