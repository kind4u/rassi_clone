import 'package:flutter/material.dart';

class MySignalTab extends StatelessWidget {
  const MySignalTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('나만의 신호'));
  }
}
