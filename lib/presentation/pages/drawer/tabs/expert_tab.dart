import 'package:flutter/material.dart';

class ExpertTab extends StatelessWidget {
  const ExpertTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('엑스퍼트 준비 중', style: TextStyle(color: Colors.grey)),
    );
  }
}
